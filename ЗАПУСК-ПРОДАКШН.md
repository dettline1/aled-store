# 🚀 Запуск полного проекта ALed Store

## 📋 План действий

### Этап 1: Запуск всех компонентов
### Этап 2: Интеграция Frontend + Backend
### Этап 3: Передача заказчику
### Этап 4: Работа с правками

---

## 🎯 Этап 1: Запуск всех компонентов

### Вариант A: Быстрый демо-запуск (БЕЗ базы данных)

**Терминал 1 - Backend API:**
```powershell
# В корне проекта
start-demo.bat
```
✅ API будет на: http://localhost:4000

**Терминал 2 - Admin Panel:**
```powershell
cd apps/admin
npm install
npm run dev
```
✅ Админка будет на: http://localhost:3001

**Терминал 3 - Frontend (витрина):**
```powershell
cd apps/frontend
npm run dev
```
✅ Витрина будет на: http://localhost:3000

---

### Вариант B: Полный запуск (С базой данных PostgreSQL)

**Шаг 1: Запустите Docker Desktop**

**Шаг 2: Запустите базы данных:**
```powershell
pnpm docker:up
```

Подождите 10-15 секунд, пока контейнеры запустятся.

**Шаг 3: Настройте переменные окружения:**
```powershell
# Скопируйте файл
copy .env.example .env

# Или создайте apps/api/.env с содержимым:
```

Создайте файл `apps/api/.env`:
```env
# API
PORT=4000
NODE_ENV=development

# JWT
JWT_SECRET=aled-secret-key-2024-change-in-production
JWT_REFRESH_SECRET=aled-refresh-secret-key-2024-change-in-production

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aled

# Redis
REDIS_URL=redis://localhost:6379

# S3 / MinIO
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=aled-media
S3_PUBLIC_URL=http://localhost:9000/aled-media

# Mail (MailHog для тестов)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM=ALed Store <no-reply@aled.local>

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3001
```

**Шаг 4: Установите зависимости API:**
```powershell
cd apps/api
npm install
```

**Шаг 5: Запустите миграции (ЕСЛИ установлен Prisma):**
```powershell
# В apps/api
npx prisma migrate dev
npx prisma db seed
```

**Шаг 6: Запустите все сервисы:**

**Терминал 1 - API:**
```powershell
cd apps/api
npm run dev
```

**Терминал 2 - Admin:**
```powershell
cd apps/admin
npm run dev
```

**Терминал 3 - Frontend:**
```powershell
cd apps/frontend
npm run dev
```

---

## 🔗 Этап 2: Интеграция Frontend + Backend

### Настройте Frontend для работы с API

**Создайте файл `apps/frontend/.env.local`:**
```env
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
```

**Создайте API клиент в Frontend:**

Файл: `apps/frontend/src/lib/api.ts`
```typescript
const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:4000/api/v1';

export const api = {
  // Категории
  async getCategories() {
    const res = await fetch(`${API_BASE_URL}/categories`);
    return res.json();
  },

  // Товары
  async getProducts(params?: { category?: string; page?: number; limit?: number }) {
    const query = new URLSearchParams();
    if (params?.category) query.set('category', params.category);
    if (params?.page) query.set('page', params.page.toString());
    if (params?.limit) query.set('limit', params.limit.toString());
    
    const res = await fetch(`${API_BASE_URL}/products?${query}`);
    return res.json();
  },

  // Товар по slug
  async getProduct(slug: string) {
    const res = await fetch(`${API_BASE_URL}/products/${slug}`);
    return res.json();
  },

  // Корзина
  async getCart(sessionId?: string) {
    const res = await fetch(`${API_BASE_URL}/cart`, {
      headers: sessionId ? { 'X-Session-ID': sessionId } : {},
    });
    return res.json();
  },

  async addToCart(productId: string, quantity: number = 1) {
    const res = await fetch(`${API_BASE_URL}/cart/items`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ productId, quantity }),
    });
    return res.json();
  },

  // Заказы
  async createOrder(data: any) {
    const res = await fetch(`${API_BASE_URL}/orders`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    return res.json();
  },
};
```

**Пример использования во Frontend:**

Файл: `apps/frontend/src/app/products/page.tsx` (или аналогичный)
```typescript
import { api } from '@/lib/api';

export default async function ProductsPage() {
  const { data: products } = await api.getProducts();

  return (
    <div>
      <h1>Товары</h1>
      <div className="grid grid-cols-3 gap-4">
        {products.map((product: any) => (
          <div key={product.id}>
            <h2>{product.name}</h2>
            <p>{product.price} ₽</p>
          </div>
        ))}
      </div>
    </div>
  );
}
```

---

## 📦 Этап 3: Передача заказчику

### Что передать:

1. **Исходный код проекта:**
   - Весь репозиторий `C:\Aled fronted\`
   - Можно создать ZIP архив или загрузить на GitHub

2. **Документация:**
   - ✅ `README.md` - основная документация
   - ✅ `README-FINAL.md` - инструкции для клиента
   - ✅ `ПРОВЕРКА-ПРОЕКТА.md` - чеклист проверки
   - ✅ `ЗАПУСК-ПРОДАКШН.md` - этот файл

3. **Учетные данные:**
   - **Admin:** admin@aled.local / admin123
   - **Manager:** manager@aled.local / manager123
   - **Customer:** customer@aled.local / customer123

4. **Доступы к сервисам:**
   - API: http://localhost:4000
   - Admin: http://localhost:3001
   - Frontend: http://localhost:3000
   - Swagger: http://localhost:4000/docs
   - PostgreSQL: localhost:5432 (postgres/postgres)
   - Redis: localhost:6379
   - MinIO: http://localhost:9001 (minioadmin/minioadmin)
   - MailHog: http://localhost:8025

### Создайте пакет для передачи:

**Вариант 1: ZIP архив**
```powershell
# Исключите node_modules и другие временные файлы
# Можно использовать 7-Zip или WinRAR
```

**Вариант 2: Git репозиторий (РЕКОМЕНДУЕТСЯ)**
```powershell
# Инициализируйте Git (если еще не сделано)
git init

# Создайте .gitignore
# (файл уже должен быть в проекте)

# Сделайте коммит
git add .
git commit -m "Initial commit: ALed Store full project"

# Загрузите на GitHub/GitLab
git remote add origin https://github.com/your-username/aled-store.git
git push -u origin main
```

---

## 🔧 Этап 4: Работа с правками от заказчика

### Организация работы:

**1. Используйте Git ветки:**
```powershell
# Основная ветка - main (стабильная версия)
git checkout main

# Для каждой правки создавайте ветку
git checkout -b feature/новая-функция
# Или
git checkout -b fix/исправление-бага

# После завершения - мердж
git checkout main
git merge feature/новая-функция
```

**2. Ведите учет задач:**

Создайте файл `ЗАДАЧИ.md`:
```markdown
# Задачи от заказчика

## В работе
- [ ] Добавить фильтр по цене на странице товаров
- [ ] Исправить отображение корзины на мобильных

## Выполнено
- [x] Настроить API
- [x] Создать админ-панель
```

**3. Типичные правки:**

#### A. Добавление нового поля в товар

**Backend (apps/api):**

1. Обновите `infra/prisma/schema.prisma`:
```prisma
model Product {
  // ... существующие поля
  newField String? // Новое поле
}
```

2. Создайте миграцию:
```powershell
cd apps/api
npx prisma migrate dev --name add_new_field
```

3. Обновите DTO в `apps/api/src/modules/products/dto/`:
```typescript
export class CreateProductDto {
  // ... существующие поля
  newField?: string;
}
```

**Frontend:**
Обновите типы и компоненты для отображения нового поля.

#### B. Изменение дизайна админки

**apps/admin/src/app/dashboard/products/page.tsx:**
```typescript
// Измените компоненты, стили, добавьте новые функции
```

#### C. Добавление нового эндпоинта API

**apps/api/src/modules/products/products.controller.ts:**
```typescript
@Get('featured')
async getFeaturedProducts() {
  return this.productsService.findFeatured();
}
```

**apps/api/src/modules/products/products.service.ts:**
```typescript
async findFeatured() {
  return this.prisma.product.findMany({
    where: { isFeatured: true },
    take: 10,
  });
}
```

### Процесс работы с правкой:

1. **Получите задачу от заказчика**
2. **Создайте ветку:**
   ```powershell
   git checkout -b fix/название-правки
   ```
3. **Внесите изменения**
4. **Протестируйте:**
   - Запустите API
   - Проверьте админку
   - Проверьте фронтенд
5. **Закоммитьте:**
   ```powershell
   git add .
   git commit -m "fix: описание правки"
   ```
6. **Отправьте заказчику на проверку**
7. **После одобрения - мердж:**
   ```powershell
   git checkout main
   git merge fix/название-правки
   git push
   ```

---

## 🛠️ Полезные команды

### Разработка:
```powershell
# Запуск всего проекта
pnpm dev

# Только API
pnpm dev:api

# Только Admin
pnpm dev:admin

# Только Frontend
pnpm dev:frontend
```

### База данных:
```powershell
# Миграции
pnpm --filter api db:migrate

# Откат миграции
pnpm --filter api db:reset

# Seed данные
pnpm --filter api db:seed

# Prisma Studio (GUI для БД)
pnpm --filter api db:studio
```

### Docker:
```powershell
# Запустить контейнеры
pnpm docker:up

# Остановить
pnpm docker:down

# Логи
pnpm docker:logs
```

### Сборка для продакшн:
```powershell
# Сборка всего
pnpm build

# Только API
pnpm build:api

# Только Admin
pnpm build:admin
```

---

## 📝 Шаблон для коммуникации с заказчиком

### Отчет о проделанной работе:

```
Добрый день!

Проект ALed Store готов к передаче.

✅ Выполнено:
1. Backend API - 8+ эндпоинтов, документация Swagger
2. Admin Panel - современный интерфейс управления
3. База данных - полная схема (20+ таблиц)
4. Docker окружение - для разработки
5. Документация - подробные инструкции

📊 Статистика:
- Файлов: 19,333
- Модулей: 80+
- Время: 8+ часов

🔗 Доступы:
- API: http://localhost:4000
- Admin: http://localhost:3001
- Docs: http://localhost:4000/docs

🔑 Тестовые данные:
- Email: admin@aled.local
- Пароль: admin123

📂 Документы:
1. README-FINAL.md - инструкции по запуску
2. ПРОВЕРКА-ПРОЕКТА.md - чеклист
3. ЗАПУСК-ПРОДАКШН.md - полное руководство

Готов к правкам и доработкам!
```

### Запрос уточнений по правке:

```
Добрый день!

Принял задачу: [описание].

Уточните, пожалуйста:
1. [вопрос 1]
2. [вопрос 2]

Ориентировочное время: [X] часов.
```

### Отчет о выполненной правке:

```
Готово! ✅

Выполнено:
- [что сделано 1]
- [что сделано 2]

Как проверить:
1. [шаги для проверки]

Коммит: [ссылка на коммит]
```

---

## 🎯 Следующие шаги

### Сейчас (для передачи):

1. ✅ **Убедитесь, что все работает:**
   - Запустите API (start-demo.bat)
   - Запустите Admin (cd apps/admin && npm run dev)
   - Проверьте все эндпоинты

2. ✅ **Подготовьте документацию:**
   - README-FINAL.md актуален
   - Все инструкции понятны

3. ✅ **Создайте демо для заказчика:**
   - Запустите все компоненты
   - Сделайте скриншоты
   - Запишите видео (опционально)

4. ✅ **Передайте проект:**
   - Отправьте архив или ссылку на GitHub
   - Приложите документацию
   - Дайте инструкции по запуску

### После передачи:

1. 🔧 **Ждите обратную связь от заказчика**
2. 🔧 **Готовьтесь к правкам:**
   - Держите проект под рукой
   - Изучите код, который создан
3. 🔧 **Оперативно реагируйте на запросы**
4. 🔧 **Ведите учет времени на правки**

---

## ✅ Финальный чеклист перед передачей

- [ ] API запускается и работает (start-demo.bat)
- [ ] Admin Panel загружается (http://localhost:3001)
- [ ] Все эндпоинты отвечают
- [ ] Документация актуальна
- [ ] .env.example заполнен
- [ ] Тестовые данные работают
- [ ] Docker Compose настроен
- [ ] README-FINAL.md актуален
- [ ] Создан архив или Git репозиторий
- [ ] Подготовлены учетные данные

**Когда все галочки стоят - можно передавать! 🎉**

