# 🚀 Обновление сайта - Инструкция

## ✅ Что было сделано:

1. ✅ **Убрана подсказка с тестовыми данными** на странице входа в админ-панель
2. ✅ **Создан новый аккаунт** для входа:
   - Email: `uvarovpaveliii@gmail.com`
   - Пароль: `jhkj281sq-sdgfwv`
3. ✅ **Обновлен логотип** - теперь желтая полоса на черном фоне
4. ✅ **Настроены баннеры** по примеру с aaled.ru (5 баннеров)

---

## 📋 Что нужно сделать НА СЕРВЕРЕ:

### Шаг 1: Подключитесь к серверу по SSH

```bash
ssh root@IP_АДРЕС_СЕРВЕРА
```

### Шаг 2: Перейдите в папку проекта

```bash
cd /var/www/aled-project
```

### Шаг 3: Загрузите изменения с GitHub

```bash
git pull origin main
```

### Шаг 4: Создайте файлы `.env.production`

Для фронтенда:
```bash
echo "NEXT_PUBLIC_API_URL=https://new.aaled.ru/api/v1" > apps/frontend/.env.production
```

Для админ-панели:
```bash
echo "NEXT_PUBLIC_API_URL=https://new.aaled.ru/api/v1" > apps/admin/.env.production
```

### Шаг 5: Пересоберите приложения

Для фронтенда:
```bash
cd /var/www/aled-project/apps/frontend
rm -rf .next node_modules/.cache
npm run build
```

Для админ-панели:
```bash
cd /var/www/aled-project/apps/admin
rm -rf .next node_modules/.cache
npm run build
```

### Шаг 6: Перезапустите все сервисы

```bash
cd /var/www/aled-project
pm2 restart all
pm2 save
```

### Шаг 7: Проверьте статус

```bash
pm2 status
pm2 logs --lines 50
```

---

## 🎯 Проверка результата:

1. **Фронтенд**: https://new.aaled.ru
   - Проверьте, что баннеры работают (должно быть 5 штук)
   - Проверьте новый логотип в шапке

2. **Админ-панель**: https://admin.new.aaled.ru
   - Войдите с новыми данными: `uvarovpaveliii@gmail.com` / `jhkj281sq-sdgfwv`
   - Проверьте раздел "Баннеры" - там должно быть 5 баннеров

---

## 📝 Новые данные для входа в админку:

- **Email**: `uvarovpaveliii@gmail.com`
- **Пароль**: `jhkj281sq-sdgfwv`

Старые данные (`admin@aled.local` / `admin123`) также работают.

---

## ⚠️ Если что-то пошло не так:

1. Проверьте логи:
```bash
pm2 logs aled-frontend --lines 50
pm2 logs aled-admin --lines 50
pm2 logs aled-api --lines 50
```

2. Перезапустите отдельные сервисы:
```bash
pm2 restart aled-frontend
pm2 restart aled-admin
pm2 restart aled-api
```

3. Проверьте Nginx:
```bash
systemctl status nginx
nginx -t
```

---

## 📸 Баннеры на главной странице:

Теперь на главной странице показываются 5 баннеров:

1. **ALED PRIME F30** - Трехчиповые модули ближнего и дальнего света
2. **Установка светодиодных BILED линз** - От 21.000 рублей под ключ
3. **Товары для установки и ремонта фар**
4. **Новые стекла фар** - В наличии более 300 моделей
5. **Дополнительные элементы освещения**

Баннеры автоматически переключаются каждые 5 секунд.

---

## 🎨 Управление баннерами:

В админ-панели есть раздел **"Баннеры"**, где можно:
- Добавлять новые баннеры
- Редактировать существующие
- Загружать изображения для баннеров
- Включать/выключать баннеры
- Менять порядок показа

---

Готово! 🎉

