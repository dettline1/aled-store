# 🚀 Пошаговая инструкция: Деплой на VPS (Вариант A)

**Выбор заказчика:**
- ✅ Вариант A: Поддомен `new.aaled.ru`
- ✅ VPS: Базовая конфигурация (1GB RAM, 500₽/мес)

---

## 📋 Общий план (6 этапов)

1. ✅ Аренда VPS сервера
2. ✅ Настройка сервера (установка ПО)
3. ✅ Деплой проекта
4. ✅ Настройка Nginx
5. ✅ Настройка SSL (HTTPS)
6. ✅ Передача данных заказчику для DNS

---

## 🎯 ЭТАП 1: Аренда VPS сервера

### Рекомендую: Timeweb (российский, простой, поддержка на русском)

#### Шаг 1.1: Регистрация на Timeweb

1. Перейдите на https://timeweb.com/ru/services/vps/
2. Нажмите "Заказать VPS"
3. Зарегистрируйтесь (email, телефон)

#### Шаг 1.2: Выбор конфигурации

**Базовая конфигурация (500₽/мес):**
- **CPU:** 1 vCPU
- **RAM:** 1 GB
- **SSD:** 10 GB
- **ОС:** Ubuntu 22.04 LTS (выбирайте это!)
- **Локация:** Москва (для РФ клиентов)

#### Шаг 1.3: Оплата и получение доступа

После оплаты вы получите на email:
- IP-адрес сервера (например: `123.45.67.89`)
- Логин: `root`
- Пароль: случайный пароль

**⚠️ ВАЖНО:** Сохраните эти данные!

---

## 🎯 ЭТАП 2: Подключение и настройка сервера

### Шаг 2.1: Подключение по SSH

#### На Windows (PowerShell):

```powershell
ssh root@ВАШ_IP_АДРЕС
```

Введите пароль из письма.

#### Первый вход - смена пароля (рекомендуется):

```bash
passwd
# Введите новый пароль два раза
```

---

### Шаг 2.2: Обновление системы

```bash
# Обновление списка пакетов
apt update

# Обновление установленных пакетов
apt upgrade -y
```

⏱️ **Время:** 3-5 минут

---

### Шаг 2.3: Установка Node.js 20.x

```bash
# Добавление репозитория Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -

# Установка Node.js и npm
apt install -y nodejs

# Проверка версии
node -v   # Должно показать v20.x.x
npm -v    # Должно показать 10.x.x
```

⏱️ **Время:** 2-3 минуты

---

### Шаг 2.4: Установка PM2 (менеджер процессов)

```bash
# Установка PM2 глобально
npm install -g pm2

# Проверка
pm2 -v
```

⏱️ **Время:** 1 минута

---

### Шаг 2.5: Установка Nginx (веб-сервер)

```bash
# Установка Nginx
apt install -y nginx

# Запуск Nginx
systemctl start nginx
systemctl enable nginx

# Проверка статуса
systemctl status nginx
```

⏱️ **Время:** 2 минуты

**Проверка:** Откройте в браузере `http://ВАШ_IP` - должна появиться страница "Welcome to nginx!"

---

### Шаг 2.6: Установка Git

```bash
# Установка Git
apt install -y git

# Проверка
git --version
```

⏱️ **Время:** 1 минута

---

## 🎯 ЭТАП 3: Деплой проекта

### Шаг 3.1: Загрузка проекта на сервер

#### Вариант 1: Через Git (если проект в GitHub/GitLab)

```bash
# Переход в директорию для веб-приложений
cd /var/www

# Клонирование репозитория
git clone https://github.com/ВАШ_USERNAME/ВАШ_REPO.git aled-project

# Переход в папку проекта
cd aled-project
```

#### Вариант 2: Через SCP (если проект локально)

**На вашем компьютере (из папки проекта):**

```powershell
# Архивирование проекта (исключая node_modules)
# Сначала убедитесь, что находитесь в папке "Aled fronted"

# Создайте временный файл со списком исключений
@"
node_modules/
.next/
.git/
"@ | Out-File -Encoding utf8 exclude.txt

# Создайте ZIP архив (используя 7-Zip или встроенный Compress-Archive)
Compress-Archive -Path * -DestinationPath aled-project.zip -Force

# Удалите временный файл
Remove-Item exclude.txt
```

**Затем загрузите на сервер:**

```powershell
# Загрузка архива на сервер
scp aled-project.zip root@ВАШ_IP:/var/www/
```

**На сервере распакуйте:**

```bash
cd /var/www
apt install -y unzip
unzip aled-project.zip -d aled-project
cd aled-project
```

⏱️ **Время:** 5-10 минут (зависит от скорости интернета)

---

### Шаг 3.2: Установка зависимостей

```bash
# Убедитесь, что находитесь в /var/www/aled-project
cd /var/www/aled-project

# Установка зависимостей (может занять 5-10 минут)
npm install

# Если возникают ошибки с памятью (для 1GB RAM):
npm install --legacy-peer-deps
```

⏱️ **Время:** 5-10 минут

---

### Шаг 3.3: Создание production конфигурации

#### Создайте файл .env для API:

```bash
nano demo-api/.env
```

Вставьте:

```env
NODE_ENV=production
PORT=4000
API_URL=http://localhost:4000
```

**Сохранение:** `Ctrl+O` → `Enter` → `Ctrl+X`

#### Создайте .env.local для frontend:

```bash
nano apps/frontend/.env.local
```

Вставьте:

```env
NEXT_PUBLIC_API_URL=http://new.aaled.ru/api
NODE_ENV=production
```

**Сохранение:** `Ctrl+O` → `Enter` → `Ctrl+X`

#### Создайте .env.local для admin:

```bash
nano apps/admin/.env.local
```

Вставьте:

```env
NEXT_PUBLIC_API_URL=http://new.aaled.ru/api
NODE_ENV=production
```

**Сохранение:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

### Шаг 3.4: Сборка проектов

```bash
# Сборка Frontend
cd /var/www/aled-project/apps/frontend
npm run build

# Сборка Admin
cd /var/www/aled-project/apps/admin
npm run build

# Возврат в корень
cd /var/www/aled-project
```

⏱️ **Время:** 3-5 минут каждый

**⚠️ ВАЖНО:** Если при сборке возникает ошибка "out of memory" (не хватает RAM), используйте временный swap:

```bash
# Создание swap файла (2GB)
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# Теперь повторите сборку
cd /var/www/aled-project/apps/frontend
npm run build

# После сборки можно отключить swap
swapoff /swapfile
rm /swapfile
```

---

### Шаг 3.5: Настройка PM2 (автозапуск приложений)

#### Создайте файл конфигурации PM2:

```bash
cd /var/www/aled-project
nano ecosystem.config.js
```

Вставьте:

```javascript
module.exports = {
  apps: [
    {
      name: 'aled-api',
      script: 'demo-api/simple-server.js',
      cwd: '/var/www/aled-project',
      env: {
        NODE_ENV: 'production',
        PORT: 4000
      },
      error_file: '/var/log/pm2/aled-api-error.log',
      out_file: '/var/log/pm2/aled-api-out.log',
      time: true
    },
    {
      name: 'aled-frontend',
      script: 'node_modules/.bin/next',
      args: 'start -p 3000',
      cwd: '/var/www/aled-project/apps/frontend',
      env: {
        NODE_ENV: 'production',
        PORT: 3000
      },
      error_file: '/var/log/pm2/aled-frontend-error.log',
      out_file: '/var/log/pm2/aled-frontend-out.log',
      time: true
    },
    {
      name: 'aled-admin',
      script: 'node_modules/.bin/next',
      args: 'start -p 3001',
      cwd: '/var/www/aled-project/apps/admin',
      env: {
        NODE_ENV: 'production',
        PORT: 3001
      },
      error_file: '/var/log/pm2/aled-admin-error.log',
      out_file: '/var/log/pm2/aled-admin-out.log',
      time: true
    }
  ]
};
```

**Сохранение:** `Ctrl+O` → `Enter` → `Ctrl+X`

#### Создайте папку для логов:

```bash
mkdir -p /var/log/pm2
```

#### Запустите приложения:

```bash
cd /var/www/aled-project

# Запуск всех приложений
pm2 start ecosystem.config.js

# Сохранение конфигурации
pm2 save

# Настройка автозапуска при перезагрузке сервера
pm2 startup
# Выполните команду, которую покажет PM2 (скопируйте и вставьте)
```

#### Проверка статуса:

```bash
pm2 status
```

Вы должны увидеть:

```
┌─────┬──────────────────┬─────────┬─────────┬──────────┐
│ id  │ name             │ status  │ restart │ uptime   │
├─────┼──────────────────┼─────────┼─────────┼──────────┤
│ 0   │ aled-api         │ online  │ 0       │ 0s       │
│ 1   │ aled-frontend    │ online  │ 0       │ 0s       │
│ 2   │ aled-admin       │ online  │ 0       │ 0s       │
└─────┴──────────────────┴─────────┴─────────┴──────────┘
```

#### Полезные команды PM2:

```bash
pm2 status              # Статус всех приложений
pm2 logs                # Просмотр логов всех приложений
pm2 logs aled-api       # Логи только API
pm2 restart all         # Перезапуск всех приложений
pm2 stop all            # Остановка всех приложений
pm2 monit               # Мониторинг в реальном времени
```

⏱️ **Время:** 3-5 минут

**Проверка:** 
- `http://ВАШ_IP:3000` - должна открыться витрина
- `http://ВАШ_IP:3001` - должна открыться админка
- `http://ВАШ_IP:4000/api/v1` - должен вернуть JSON с API endpoints

---

## 🎯 ЭТАП 4: Настройка Nginx (проксирование на домен)

### Шаг 4.1: Создание конфигурации для Frontend

```bash
nano /etc/nginx/sites-available/aled-frontend
```

Вставьте:

```nginx
server {
    listen 80;
    server_name new.aaled.ru;

    # Логи
    access_log /var/log/nginx/aled-frontend-access.log;
    error_log /var/log/nginx/aled-frontend-error.log;

    # API проксирование
    location /api {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Увеличение лимита для загрузки изображений
        client_max_body_size 50M;
    }

    # Frontend проксирование
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Сохранение:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

### Шаг 4.2: Создание конфигурации для Admin

```bash
nano /etc/nginx/sites-available/aled-admin
```

Вставьте:

```nginx
server {
    listen 80;
    server_name admin.new.aaled.ru;

    # Логи
    access_log /var/log/nginx/aled-admin-access.log;
    error_log /var/log/nginx/aled-admin-error.log;

    # API проксирование
    location /api {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Увеличение лимита для загрузки изображений
        client_max_body_size 50M;
    }

    # Admin проксирование
    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Сохранение:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

### Шаг 4.3: Активация конфигураций

```bash
# Удаление дефолтной конфигурации
rm /etc/nginx/sites-enabled/default

# Создание символических ссылок
ln -s /etc/nginx/sites-available/aled-frontend /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/aled-admin /etc/nginx/sites-enabled/

# Проверка конфигурации Nginx
nginx -t
```

Должно показать:

```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

```bash
# Перезапуск Nginx
systemctl restart nginx

# Проверка статуса
systemctl status nginx
```

⏱️ **Время:** 3 минуты

---

## 🎯 ЭТАП 5: Настройка SSL (HTTPS) с Let's Encrypt

### Шаг 5.1: Установка Certbot

```bash
# Установка Certbot и плагина для Nginx
apt install -y certbot python3-certbot-nginx
```

⏱️ **Время:** 2 минуты

---

### Шаг 5.2: Получение SSL сертификатов

**⚠️ ВАЖНО:** Перед этим шагом заказчик должен настроить DNS записи!

Проверьте, что домены указывают на ваш IP:

```bash
# Проверка DNS
ping new.aaled.ru
ping admin.new.aaled.ru
```

Если домены резолвятся на ваш IP, продолжайте:

```bash
# Получение сертификатов для обоих доменов
certbot --nginx -d new.aaled.ru -d admin.new.aaled.ru
```

Вам нужно будет:
1. Ввести email (для уведомлений о продлении)
2. Согласиться с условиями (Y)
3. Выбрать, получать ли новости (N можно)
4. Certbot автоматически настроит Nginx

⏱️ **Время:** 3-5 минут

---

### Шаг 5.3: Настройка автообновления сертификатов

```bash
# Тестирование автообновления
certbot renew --dry-run
```

Если тест прошел успешно, сертификаты будут автоматически обновляться.

---

### Шаг 5.4: Проверка HTTPS

Откройте в браузере:
- `https://new.aaled.ru` - должна быть витрина (с замочком 🔒)
- `https://admin.new.aaled.ru` - должна быть админка (с замочком 🔒)

⏱️ **Время:** 2 минуты

---

## 🎯 ЭТАП 6: Передача данных заказчику

### Шаг 6.1: Создайте инструкцию для заказчика

```bash
nano /root/dns-instruction.txt
```

Вставьте (замените ВАШ_IP на реальный IP):

```
========================================
ИНСТРУКЦИЯ ПО НАСТРОЙКЕ DNS
========================================

Ваш IP-адрес сервера: ВАШ_IP

Что нужно сделать:
1. Зайдите на reg.ru
2. Войдите в личный кабинет
3. Выберите домен aaled.ru
4. Перейдите в "DNS-серверы и зона"
5. Нажмите "Изменить зону"

Добавьте следующие DNS записи:

========================================
Запись 1 (Витрина):
========================================
Тип записи: A
Поддомен: new
IP-адрес: ВАШ_IP
TTL: 3600

========================================
Запись 2 (Админка):
========================================
Тип записи: A
Поддомен: admin.new
IP-адрес: ВАШ_IP
TTL: 3600

========================================

После добавления записей:
- Изменения вступят в силу через 1-2 часа
- Сайт будет доступен по адресам:
  • Витрина: https://new.aaled.ru
  • Админка: https://admin.new.aaled.ru

========================================
```

**Сохранение:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

### Шаг 6.2: Отправьте заказчику

Скопируйте содержимое файла:

```bash
cat /root/dns-instruction.txt
```

Отправьте заказчику этот текст + скриншоты примера настройки DNS.

---

## 📊 Чеклист готовности

Перед передачей заказчику убедитесь:

- [ ] VPS арендован и доступен
- [ ] Все ПО установлено (Node.js, PM2, Nginx, Git)
- [ ] Проект загружен и собран
- [ ] PM2 запущен, все приложения в статусе "online"
- [ ] Nginx настроен и работает
- [ ] Сайты доступны по IP:3000 и IP:3001
- [ ] Certbot установлен (готов для SSL после настройки DNS)
- [ ] Инструкция для заказчика готова
- [ ] IP-адрес сервера записан

---

## 📝 Что сказать заказчику

```
Здравствуйте!

Сервер готов! 🎉

Чтобы сайт заработал на домене new.aaled.ru, нужно настроить DNS записи.

IP-адрес сервера: ВАШ_IP

Вам нужно добавить 2 DNS записи в панели Reg.ru:

1. Запись для витрины:
   Тип: A
   Поддомен: new
   IP: ВАШ_IP
   TTL: 3600

2. Запись для админки:
   Тип: A
   Поддомен: admin.new
   IP: ВАШ_IP
   TTL: 3600

После настройки DNS (через 1-2 часа) сайт будет доступен:
• Витрина: https://new.aaled.ru
• Админка: https://admin.new.aaled.ru

Я настрою SSL сразу после того, как DNS записи начнут работать.

Если нужна помощь с настройкой DNS - дайте знать!
```

---

## 🔧 Полезные команды для управления

### Управление PM2:

```bash
pm2 status              # Статус приложений
pm2 restart all         # Перезапуск всех
pm2 logs                # Просмотр логов
pm2 monit               # Мониторинг
```

### Управление Nginx:

```bash
systemctl status nginx  # Статус Nginx
systemctl restart nginx # Перезапуск Nginx
nginx -t                # Проверка конфигурации
```

### Просмотр логов:

```bash
# Логи PM2
pm2 logs

# Логи Nginx
tail -f /var/log/nginx/aled-frontend-access.log
tail -f /var/log/nginx/aled-frontend-error.log
```

### Обновление проекта:

```bash
cd /var/www/aled-project
git pull                # Если через Git
npm install             # Обновление зависимостей
cd apps/frontend && npm run build
cd ../admin && npm run build
pm2 restart all         # Перезапуск приложений
```

---

## 🆘 Решение проблем

### Проблема: Приложение не запускается

```bash
pm2 logs aled-frontend  # Смотрим логи
pm2 restart aled-frontend  # Перезапускаем
```

### Проблема: 502 Bad Gateway

```bash
# Проверяем статус приложений
pm2 status

# Перезапускаем
pm2 restart all

# Проверяем логи
pm2 logs
```

### Проблема: Не хватает памяти при сборке

```bash
# Создаем временный swap
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# Собираем проект
cd /var/www/aled-project/apps/frontend
npm run build

# Отключаем swap
swapoff /swapfile
rm /swapfile
```

---

## ⏱️ Итоговое время деплоя

- **Аренда VPS:** 10-15 минут
- **Настройка сервера:** 15-20 минут
- **Деплой проекта:** 20-30 минут
- **Настройка Nginx:** 5-10 минут
- **Ожидание DNS:** 1-2 часа (заказчик)
- **Настройка SSL:** 5 минут

**ИТОГО:** ~1 час активной работы + 1-2 часа ожидания DNS

---

## ✅ Готово!

После выполнения всех этапов у вас будет:
- ✅ Работающий VPS сервер
- ✅ Настроенные приложения (API, Frontend, Admin)
- ✅ Nginx с проксированием
- ✅ SSL сертификаты (HTTPS)
- ✅ Автозапуск при перезагрузке сервера
- ✅ Рабочие сайты на new.aaled.ru и admin.new.aaled.ru

**Удачи с деплоем!** 🚀

