# 🚀 ВЫПУСК САЙТА В ПРОДАКШН

## Пошаговая инструкция для выпуска проекта ALED

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ

### Этап 1: Подготовка проекта ✅ (УЖЕ СДЕЛАНО)
- ✅ Разработка завершена
- ✅ Все функции работают локально
- ✅ API, Админка, Фронтенд готовы

### Этап 2: Настройка сервера (НУЖНО СДЕЛАТЬ)
### Этап 3: База данных (НУЖНО СДЕЛАТЬ)
### Этап 4: Деплой (НУЖНО СДЕЛАТЬ)
### Этап 5: Домен и SSL (НУЖНО СДЕЛАТЬ)

---

## 🎯 ВАРИАНТ A: БЫСТРЫЙ СТАРТ (Vercel + Суpabase) - РЕКОМЕНДУЕТСЯ

### Преимущества:
- ⚡ Быстро (30 минут)
- 🆓 Бесплатно (для старта)
- 🔄 Автоматические обновления
- 🌐 CDN по всему миру
- 🔒 Автоматический SSL

### Что нужно:
1. **Домен** (aaled.ru) - должен быть у заказчика
2. **Аккаунт Vercel** (vercel.com)
3. **Аккаунт Supabase** (supabase.com) или другая БД
4. **GitHub** репозиторий

### Шаги:

#### 1️⃣ Создайте GitHub репозиторий
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ВАШ-ЛОГИН/aled-store.git
git push -u origin main
```

#### 2️⃣ База данных (Supabase - БЕСПЛАТНО)

**a) Зарегистрируйтесь:** https://supabase.com

**b) Создайте новый проект:**
- Project name: aled-store
- Database password: (придумайте сложный)
- Region: Europe (ближе к России)

**c) Получите параметры подключения:**
- Settings → Database → Connection string
- Скопируйте URI (будет нужен)

**d) Создайте таблицы:**
```sql
-- Скопируйте схему из файла:
-- apps/api/prisma/schema.prisma
-- И выполните миграции через Prisma
```

#### 3️⃣ Деплой Backend (Vercel)

**a) Перейдите на:** https://vercel.com

**b) Import Git Repository:**
- Выберите ваш репозиторий
- Framework Preset: Other
- Root Directory: `apps/api`

**c) Environment Variables:**
```env
DATABASE_URL=postgresql://...ваш-url-из-supabase
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
REDIS_URL=redis://...если-нужен
S3_BUCKET=aled-media
```

**d) Deploy!**

**e) Скопируйте URL API:** например `https://aled-api.vercel.app`

#### 4️⃣ Деплой Frontend (Vercel)

**a) New Project → Import Repository**

**b) Settings:**
- Root Directory: `apps/frontend`
- Framework: Next.js

**c) Environment Variables:**
```env
NEXT_PUBLIC_API_URL=https://aled-api.vercel.app
```

**d) Deploy!**

**e) URL:** например `https://aled-store.vercel.app`

#### 5️⃣ Деплой Admin Panel (Vercel)

**a) New Project → Import Repository**

**b) Settings:**
- Root Directory: `apps/admin`
- Framework: Next.js

**c) Environment Variables:**
```env
NEXT_PUBLIC_API_URL=https://aled-api.vercel.app
```

**d) Deploy!**

**e) URL:** например `https://aled-admin.vercel.app`

#### 6️⃣ Настройка домена

**В Vercel:**
- Domains → Add Domain
- Frontend: aaled.ru → указать на `aled-store.vercel.app`
- Admin: admin.aaled.ru → указать на `aled-admin.vercel.app`
- API: api.aaled.ru → указать на `aled-api.vercel.app`

**У регистратора домена (где куплен aaled.ru):**
```
Тип    Имя      Значение
A      @        76.76.21.21 (IP Vercel)
CNAME  admin    cname.vercel-dns.com
CNAME  api      cname.vercel-dns.com
```

#### 7️⃣ Обновите URLs после настройки домена

В Vercel → Settings → Environment Variables:

**Frontend:**
```env
NEXT_PUBLIC_API_URL=https://api.aaled.ru
```

**Admin:**
```env
NEXT_PUBLIC_API_URL=https://api.aaled.ru
```

**Redeploy** все 3 проекта!

---

## 🎯 ВАРИАНТ B: VPS/Dedicated Server (Более сложный)

### Преимущества:
- 🎛️ Полный контроль
- 💪 Больше ресурсов
- 🔧 Гибкая настройка

### Что нужно:
1. **VPS** (например, timeweb.ru, beget.ru, reg.ru)
2. **Ubuntu 22.04** или новее
3. **SSH доступ**
4. **Домен** настроен на IP сервера

### Шаги:

#### 1️⃣ Подключитесь к серверу
```bash
ssh root@ВАШ_IP
```

#### 2️⃣ Установите необходимое ПО
```bash
# Обновление системы
apt update && apt upgrade -y

# Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# PostgreSQL
apt install -y postgresql postgresql-contrib

# Nginx
apt install -y nginx

# PM2 (менеджер процессов)
npm install -g pm2

# Git
apt install -y git
```

#### 3️⃣ Настройте PostgreSQL
```bash
sudo -u postgres psql

CREATE DATABASE aled_store;
CREATE USER aled_user WITH ENCRYPTED PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE aled_store TO aled_user;
\q
```

#### 4️⃣ Клонируйте проект
```bash
cd /var/www
git clone https://github.com/ВАШ-ЛОГИН/aled-store.git
cd aled-store
```

#### 5️⃣ Настройте окружение

**Backend .env:**
```bash
cd /var/www/aled-store/apps/api
nano .env
```
```env
DATABASE_URL="postgresql://aled_user:ваш_пароль@localhost:5432/aled_store"
JWT_SECRET="очень-длинный-случайный-ключ-минимум-32-символа"
PORT=4000
NODE_ENV=production
```

**Frontend .env:**
```bash
cd /var/www/aled-store/apps/frontend
nano .env.production
```
```env
NEXT_PUBLIC_API_URL=https://api.aaled.ru
```

**Admin .env:**
```bash
cd /var/www/aled-store/apps/admin
nano .env.production
```
```env
NEXT_PUBLIC_API_URL=https://api.aaled.ru
```

#### 6️⃣ Установите зависимости и соберите проекты
```bash
cd /var/www/aled-store

# Backend
cd apps/api
npm install --production
npx prisma generate
npx prisma migrate deploy
npm run build

# Frontend
cd ../frontend
npm install --production
npm run build

# Admin
cd ../admin
npm install --production
npm run build
```

#### 7️⃣ Запустите с PM2
```bash
# Backend
cd /var/www/aled-store/apps/api
pm2 start npm --name "aled-api" -- start

# Frontend
cd /var/www/aled-store/apps/frontend
pm2 start npm --name "aled-frontend" -- start

# Admin
cd /var/www/aled-store/apps/admin
pm2 start npm --name "aled-admin" -- start

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

#### 8️⃣ Настройте Nginx

**API:**
```bash
nano /etc/nginx/sites-available/api.aaled.ru
```
```nginx
server {
    listen 80;
    server_name api.aaled.ru;

    location / {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Frontend:**
```bash
nano /etc/nginx/sites-available/aaled.ru
```
```nginx
server {
    listen 80;
    server_name aaled.ru www.aaled.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Admin:**
```bash
nano /etc/nginx/sites-available/admin.aaled.ru
```
```nginx
server {
    listen 80;
    server_name admin.aaled.ru;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Активируйте конфиги:**
```bash
ln -s /etc/nginx/sites-available/api.aaled.ru /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/aaled.ru /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/admin.aaled.ru /etc/nginx/sites-enabled/

nginx -t
systemctl restart nginx
```

#### 9️⃣ Установите SSL (Let's Encrypt - БЕСПЛАТНО)
```bash
apt install -y certbot python3-certbot-nginx

certbot --nginx -d aaled.ru -d www.aaled.ru
certbot --nginx -d api.aaled.ru
certbot --nginx -d admin.aaled.ru
```

#### 🔟 Проверьте статус
```bash
pm2 status
systemctl status nginx
```

---

## 📱 ВАРИАНТ C: Для ДЕМО (самый простой)

Если нужно просто показать заказчику:

### Railway.app (1 клик деплой)
1. Зарегистрируйтесь на railway.app
2. New Project → Deploy from GitHub
3. Добавьте переменные окружения
4. Deploy!

### Render.com (тоже просто)
1. render.com → New Web Service
2. Подключите GitHub
3. Настройте переменные
4. Deploy!

---

## ⚙️ ВАЖНЫЕ НАСТРОЙКИ ДЛЯ ПРОДАКШН

### 1. Измените в API (apps/api/.env):
```env
NODE_ENV=production
JWT_SECRET=используйте-генератор-паролей-32+ символа
DATABASE_URL=реальная-база-данных
```

### 2. Отключите demo-api:
- В продакшне НЕ используйте `demo-api/simple-server.js`
- Используйте полноценный API из `apps/api`

### 3. Настройте CORS правильно:
```typescript
// apps/api/src/main.ts
app.enableCors({
  origin: [
    'https://aaled.ru',
    'https://www.aaled.ru',
    'https://admin.aaled.ru'
  ],
  credentials: true,
});
```

### 4. Безопасность:
- ✅ Смените все пароли
- ✅ Используйте сильные JWT секреты
- ✅ Включите HTTPS везде
- ✅ Настройте firewall
- ✅ Регулярные бэкапы БД

---

## 🔄 ОБНОВЛЕНИЕ ПРОЕКТА

### Vercel (автоматически):
- Просто сделайте `git push` - всё обновится само!

### VPS (вручную):
```bash
cd /var/www/aled-store
git pull
cd apps/api && npm install && npm run build && pm2 restart aled-api
cd ../frontend && npm install && npm run build && pm2 restart aled-frontend
cd ../admin && npm install && npm run build && pm2 restart aled-admin
```

---

## 💰 ПРИМЕРНАЯ СТОИМОСТЬ

### Вариант A (Vercel + Supabase):
- **Домен:** ~500₽/год
- **Hosting:** 0₽ (бесплатно до ~100K запросов/месяц)
- **Итого:** ~500₽/год для старта

### Вариант B (VPS):
- **Домен:** ~500₽/год
- **VPS:** от 200₽/месяц (timeweb, beget)
- **Итого:** ~2900₽/год

---

## ☎️ ЧТО СКАЗАТЬ ЗАКАЗЧИКУ

**Вариант 1 - Вы всё делаете сами:**
> "Для выпуска сайта в продакшн нужно:
> 1. Домен (aaled.ru) - 500₽/год
> 2. Хостинг - я рекомендую Vercel (бесплатно для старта)
> 3. База данных - Supabase (бесплатно до 500MB)
> 
> Я настрою всё за вас. Нужен только доступ к домену.
> Дополнительная стоимость: [ВАША ЦЕНА] за настройку и деплой."

**Вариант 2 - Заказчик сам выбирает:**
> "Проект готов к выпуску. Есть 3 варианта хостинга:
> 
> 1. **Vercel** (рекомендую) - бесплатно, быстро, автообновления
> 2. **VPS** - больше контроля, от 200₽/мес
> 3. **Railway/Render** - средний вариант
> 
> Я могу настроить любой вариант. Стоимость настройки: [ВАША ЦЕНА]"

---

## 📋 ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

- [ ] Все функции работают локально
- [ ] База данных настроена
- [ ] API запущен и доступен
- [ ] Frontend работает с продакшн API
- [ ] Admin панель работает с продакшн API
- [ ] SSL сертификаты установлены
- [ ] Домен настроен правильно
- [ ] Изменён дефолтный пароль админа
- [ ] Настроены бэкапы БД
- [ ] Проверена безопасность
- [ ] Заказчик протестировал

---

## 🆘 ПРОБЛЕМЫ И РЕШЕНИЯ

**API не доступен:**
- Проверьте `pm2 logs aled-api` или логи Vercel
- Проверьте настройки CORS
- Проверьте подключение к БД

**Frontend не видит API:**
- Проверьте `NEXT_PUBLIC_API_URL`
- Проверьте CORS на API
- Откройте консоль браузера (F12)

**База данных:**
- Проверьте `DATABASE_URL`
- Убедитесь что миграции применены
- Проверьте пароль и права доступа

---

## 📞 КОНТАКТЫ ДЛЯ ПОМОЩИ

- **Vercel Support:** vercel.com/support
- **Supabase Docs:** supabase.com/docs
- **Next.js Deployment:** nextjs.org/docs/deployment

---

**Рекомендую: ВАРИАНТ A (Vercel) для быстрого старта!**

Нужна помощь с конкретным вариантом? Спрашивайте! 🚀

